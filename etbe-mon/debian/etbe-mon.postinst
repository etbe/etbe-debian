#!/bin/bash
set -e

# if upgrade from before version 0.2.5 then migrate xmpp config
if [ "$1" = "configure" ] && dpkg --compare-versions "$2" lt 0.2.5; then
  /sbin/etbe-mon-upgrade-xmpp root
  /sbin/etbe-mon-upgrade-xmpp mon
fi
